import styled from "styled-components";

export const StyledTeachers = styled.div`
  .labelsWrapper {
    display: flex;
    gap: 20px;
  }
  .labelText {
    margin-bottom: 8px;
    font-weight: 400;
    font-size: 14px;
    line-height: 1.29;
    color: #8a8a89;
  }
  .label {
    display: flex;
    flex-direction: column;
    max-width: 220px;
  }
  .select-launuage {
  }
`;